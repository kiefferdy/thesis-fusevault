# Frontend Environment Variables (Local Development)

# API Configuration - for local development only
VITE_API_URL=http://localhost:8000

# Note: For Docker deployments, the API URL is set via build arguments 
# in docker-compose files, not this .env file